Place three short, warm UI sounds here.

- `chime.wav` / `chime.mp3`: soft save success, around 400ms
- `whoosh.wav` / `whoosh.mp3`: subtle page transition, around 300ms
- `pop.wav` / `pop.mp3`: tiny toast appear sound, around 150ms

The generated placeholders in this folder are intentionally quiet and can be
replaced with CC0 MP3 or WAV assets from a trusted source.
